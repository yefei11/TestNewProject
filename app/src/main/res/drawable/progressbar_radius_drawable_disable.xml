<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 背景  gradient是渐变,corners定义的是圆角 -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />

            <solid android:color="@color/main_divide_gray" />
        </shape>
    </item>
    <!-- 第二条进度条颜色 -->
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="10dip" />

                <gradient
                    android:centerColor="@color/main_divide_gray"
                    android:centerY="0.45"
                    android:endColor="@color/main_divide_gray"
                    android:startColor="@color/main_divide_gray" />
            </shape>
        </clip>
    </item>
    <!-- 进度条 -->
    <!--   <item android:id="@android:id/progress">
           <clip>
               <shape>
                  &lt;!&ndash; <corners android:radius="4dip" />&ndash;&gt;

                   <solid android:color="@color/pb_second" />
               </shape>
           </clip>
       </item>-->

</layer-list>